{
  "name": "self_organizing_maps",
  "root": "packages/self_organizing_maps", 
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["python -m pip install build", "python -m build"],
        "cwd": "packages/self_organizing_maps"
      }
    },
    "test": {
      "executor": "nx:run-commands", 
      "options": {
        "commands": ["python -m pytest . -v"],
        "cwd": "packages/self_organizing_maps"
      }
    }
  }
}
